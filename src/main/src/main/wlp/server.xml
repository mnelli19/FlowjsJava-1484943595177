<server>

    <featureManager>
        <feature>servlet-3.1</feature>
    </featureManager>

    <httpEndpoint id="defaultHttpEndpoint"
                  host="*"
                  httpPort="9080">
        <tcpOptions soReuseAddr="true"/>
    </httpEndpoint>

    <application name="FlowJs-Java-Bluemix" context-root="/FlowJs-Java-Bluemix" location="${appLocation}" type="war"/>

</server>
